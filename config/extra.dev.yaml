# Homalos期货量化交易系统配置文件
# 配置文件格式参考：https://yaml.org/spec/1.2/spec.html

# 系统基础配置
base:
  data_dir: "data"          # 数据存储目录
  log_dir: "logs"           # 日志存储目录
  flow_dir: "con"           # 数据流存储目录
  timezone: "Asia/Shanghai"   # 系统时区
# 交易时段配置
trading_hours:
  enable_check: false         # 启用交易时段检查
  futures:                    # 期货交易时段配置，格式为[["开始时间", "结束时间"], ...]
    - ["09:00", "10:15"]      # 早盘
    - ["10:30", "11:30"]      # 早盘
    - ["13:30", "15:00"]      # 午盘
    - ["21:00", "23:00"]      # 夜盘(大部分品种)
    - ["21:00", "02:30"]      # 夜盘(某些品种)
